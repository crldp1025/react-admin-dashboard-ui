import React from 'react';
import SiteContainer from '../components/SiteContainer';

const Settings = () => {
  return (
    <SiteContainer>
      Settings
    </SiteContainer>
  );
};

export default Settings;