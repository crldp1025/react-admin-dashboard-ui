import React from 'react';
import SiteContainer from '../components/SiteContainer';

const Dashboard = () => {
  return (
    <SiteContainer>
      Dashboard
    </SiteContainer>
  );
};

export default Dashboard;