import React from 'react';
import SiteContainer from '../components/SiteContainer';

const Users = () => {
  return (
    <SiteContainer>
      Users
    </SiteContainer>
  );
};

export default Users;